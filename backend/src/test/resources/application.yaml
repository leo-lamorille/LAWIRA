spring:
  datasource:
    url: jdbc:h2:file:./test-db
    username: test
    password: test
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
      show-sql: true

  liquibase:
    change-log: db/changelog-master-test.xml

jwt:
  secret: "This is a secret"
